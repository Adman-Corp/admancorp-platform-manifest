environment: dev
cloud: azure
clusterSecretStore:
  enabled: true
  provider: azure

envoy-gateway:
  domain: dev.admancorp.com
  envoyServiceHostname: az-gw.dev.admancorp.com

kube-prometheus-stack:
  enabled: true

loki:
  enabled: true

alloy:
  enabled: true
